# Prompt sheet — Rebuild / Settle

Paste-ready. Use with `00-shared-base.md` rules.

## Subject

Post-stamp rebuild: dark sci-fi clicker HUD reconstructing outward from center; panels sliding into place; residual sparkles fading; worldline accent still present but calming toward playable UI idle.

## View

Fullscreen orthographic UI rebuild, center → edges assembly. No characters. Stamp may be small residual watermark at center (abstract).

## Canvas

Primary 1920×1080. Alternate 1080×1920 same outward rebuild.

## Palette roles (PROVISIONAL)

- `role_void` / `role_ui_ink` #05070D / #0B0F1A
- `role_highlight_white` #E8F4FF
- Worldline accent: use one only depending on variant — Pulse cyan, AURELIA gold, Resonance magenta, or Core orange (PROVISIONAL hex from stamp sheets)
- For a **shared** rebuild plate: cool white + dim cyan rim only; leave accent slots empty for runtime tint

## Outline / light

Clean panel edges locking; soft residual bloom decreasing; readable negative space for real HUD widgets; no muddy fog.

## Motion beats / frames (SUGGESTED)

SUGGESTED: 10 frames — panels fly in → snap → sparkles fade → idle. Or two stills: `rebuild` mid-assembly + `settle` almost-idle.

## Filenames

- Shared: `rebirth_rebuild_shared_v1.png`, `rebirth_settle_shared_v1.png`
- Optional tinted: `rebirth_rebuild_[worldline]_v1.png`

## Paste prompt — shared rebuild (copy block)

```
Dark sci-fi clicker game HUD rebuilding outward from center after rebirth, UI panels sliding into place, residual neon sparkles fading, near-black #05070D and ink #0B0F1A plates, soft white rim #E8F4FF, orthographic flat UI, high contrast readable margins for widgets, no readable text, no characters, calm settling VFX still, 1920x1080
```

## Paste prompt — settle (copy block)

```
Dark sci-fi clicker game UI almost idle after rebirth settle, clean HUD plates in place, minimal residual glow, near-black background, subtle neon rim only, orthographic flat UI, high contrast, empty widget regions, no readable text, no characters, 1920x1080
```
